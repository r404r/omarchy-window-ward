# Contributing

Keep the close path small, deterministic and independent from the shell UI. Add fixture tests for
every matching or setup change. Do not collect window titles, URLs, profiles or credentials.

Run the commands in the README Development section before submitting a change. Commits should be
signed when possible and changes should include an entry under `CHANGELOG.md`.
