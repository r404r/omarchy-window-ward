#!/usr/bin/env python3
import sys

# These entrypoints run from the plugin checkout, which Quickshell watches.
# Do this before importing the shared module so invoking setup cannot create
# a __pycache__ reload event there.
sys.dont_write_bytecode = True
from window_ward_integration import main
main("setup")
